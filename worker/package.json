{
  "name": "pdf-packet-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for PDF packet generation",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241011.0",
    "wrangler": "^3.78.12",
    "typescript": "^5.6.3"
  }
}
